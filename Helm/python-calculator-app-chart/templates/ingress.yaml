apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: python-calculator-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: python-calculator-app.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: python-calculator-app
                port:
                  number: 80
